<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjtu.dao.ScDao">


    <select id="findCourseInfoBySno" resultType="java.util.Map">
        SELECT sc.cno,cname,score
        FROM sc
        JOIN course ON sc.cno = course.cno
        WHERE sno = #{sno}
    </select>

    <insert id="addStudentCourse">
        insert into sc(sno,cno,score)
        values (#{id}, #{cno},#{score})
    </insert>

    <delete id="deleteStudentCourse">
        delete from sc where sno = #{id} and cno = #{cno}
    </delete>


    <!--    根据学生id查找该学生没有选的课程，返回符合条件的course和老师姓名-->
    <select id="findCourseNotSelected" resultType="java.util.Map">
        SELECT course.*,teacher.name as teacherName
        FROM course
        LEFT JOIN teacher ON course.tno = teacher.id
        WHERE course.cno NOT IN (
            SELECT sc.cno
            FROM sc
        WHERE sc.sno = #{id}



















        )
    </select>


</mapper>