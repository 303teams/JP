<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjtu.dao.ContentDao">


    <insert id="insert" parameterType="Content" useGeneratedKeys="true" keyProperty="contentID">
        insert into content(contentID,cno,sno,homeworkID,content,fileName)
        values (#{contentID}, #{cno},#{sno},#{homeworkID},#{content},#{fileName})
    </insert>

    <select id="findCTById" resultType="Content">
        SELECT *
        FROM content
        WHERE content.contentID = #{id}
    </select>

</mapper>